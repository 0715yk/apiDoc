/**
 * 숫자를 더합니다.
 *
 * @alpha
 * @param x - 첫 번째 입력 숫자
 * @param y - 두 번째 입력 숫자
 * @returns x와 y의 합
 *
 * @example
 * ```typescript
 * const result = addNumbers(1, 2);
 * console.log(result); // 3
 * ```
 */
export function addNumbers(x: number, y: number): number {
  return x + y;
}
